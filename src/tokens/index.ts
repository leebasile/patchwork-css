/**
 * Public API for the tokens module.
 */

export { resolveTokens } from './resolver';
export type {
  TokenValue,
  TokenScale,
  DesignTokenConfig,
  ResolvedToken,
  TokenResolver,
} from './types';
