/**
 * compose — deterministic CSS class set composition
 *
 * @module
 */
export { compose } from "./compose";
export type {
  ComposeConfig,
  ClassVariantMap,
  CompoundVariant,
  VariantSelection,
  ComposedClasses,
} from "./types";
